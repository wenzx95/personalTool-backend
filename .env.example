# 环境变量配置示例 - Docker 部署
# 复制此文件为 .env 并填入实际配置值

# MySQL 数据库配置（连接到 Docker 网络中的 MySQL 容器）
DB_USERNAME=root
DB_PASSWORD=your-strong-password-here
MYSQL_ROOT_PASSWORD=your-strong-root-password-here

# JWT密钥（至少32个字符）
JWT_SECRET=your-jwt-secret-key-at-least-32-characters-long

# 加密密钥（必须是32个字符）
APP_ENCRYPTION_KEY=your-encryption-key-must-be-32-chars

# Python服务地址（连接到 Docker 网络中的 Python 容器）
PYTHON_STOCK_SERVICE_URL=http://python-stock-service:8000

# JVM参数（针对阿里云1核2G内存优化）
JAVA_OPTS=-Xms256m -Xmx512m -XX:+UseG1GC -XX:+UseContainerSupport -XX:MaxRAMPercentage=60.0
